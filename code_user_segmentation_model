# 导入pandas模块，简写为pd
import pandas as pd

'''获取描绘R、F、M的数据'''
# 1.1 读取并处理数据集
# 读取文件，赋值给df
df = pd.read_csv(r"D:\tools\Programming\VSCODE\python_vscode\Files_RequiredforPractice\cross_border_ecommerce_platform_user_data\user_info\user_info\user_info.csv")

# 将"last_order_date"这一列，转为时间类型
df["last_order_date"] = pd.to_datetime(df["last_order_date"])

# 输出df
#print(df)

# 1.2 获取描绘R的数据
# 导入datetime模块中的datetime
from datetime import datetime

# 使用datetime()函数，构建2019年4月1日的时间，赋值给endTime
endTime = datetime(2019,4,1)

# 计算endTime和"last_order_date"这一列的时间间隔
# 将结果添加为df的"time_gap"列
df["time_gap"] = endTime - df["last_order_date"]

# 通过.dt.days，将"time_gap"列的天数提取出来
df["time_gap"] = df["time_gap"].dt.days


# 输出time_gap这一列
#print(df["time_gap"])


'''对R、F、M进行描述性分析'''
# 导入matplotlib.pyplot模块，简写为plt
#import matplotlib.pyplot as plt

# 通过给 plt.rcParams["font.sans-serif"] 赋值
# 将字体设置为 Microsoft YaHei 
#plt.rcParams["font.sans-serif"] = "Microsoft YaHei"


# 2.1 对R进行描述性分析
# 根据time_gap分组聚合，赋值给R
#R = df["time_gap"].groupby(df["time_gap"]).count()

# 计算各部分人数，对应的比例
#R_percent = R / 51394

# 输出R_percent
#print(R_percent)

# TODO 使用plt.bar()函数
# 以R_percent的index为x轴的值，R_percent的values为y轴的值
#plt.bar(R_percent.index,R_percent.values)


# 2.2 对F进行描述性分析
# TODO 根据order_count分组聚合，赋值给F
#F = df["order_count"].groupby(df["order_count"]).count()

# TODO 计算各部分人数，对应的比例，赋值给F_percent
#F_percent = F /51394

# TODO 使用plt.bar()函数
# 以F_percent的index为x轴的值，F_percent的values为y轴的值
#plt.bar(F_percent.index, F_percent.values)

# 2.3 对M进行描述性分析
# TODO 根据total_amount分组聚合，赋值给M
#M = df["total_amount"].groupby(df["total_amount"]).count()

# 计算各部分人数对应的比例，赋值给M_percent
#M_percent = M / 51394

# 使用plt.bar()函数
# 以M_percent的index为x轴的值，M_percent的values为y轴的值
#plt.bar(M_percent.index, M_percent.values)

# 用show()函数展示图像
#plt.show()

#由于x轴的区间跨度太大了，导致单一的值展示在图上依然非常不起眼。别说分布情况，连蓝色的柱子都很难从图像里被发现。
#这时，就可以考虑换一种图像和方式来观察数据的趋势，如直方图。

# TODO 使用plt.hist()函数
# 将df["total_amount"]这一列数据传入，并传入参数bins =100
#plt.hist(df["total_amount"], bins=100)

# 用show()函数展示图像
#plt.show()

'''依次划分R、F、M'''
# 使用cut()函数，对"time_gap"进行数据分箱
# 以0,50,100,200,300,365为边界分为5组，将结果添加为df的"R"列
#df["R"] = pd.cut(df["time_gap"], [0,50,100,200,300,365])

# 使用print输出df["R"]
#print(df["R"])

# TODO 使用value_counts()函数
# 计算df["R"]每个区间的值，赋值给R_value
#R_value=df["R"].value_counts()

# 使用print输出R_value
#print(R_value)

# 使用qcut()函数，对"time_gap"进行数据分箱
# 均分为5组，区间标记命名为5-1，赋值给df["R"]
#df["R"]=pd.qcut(df["time_gap"],q=5,labels=[5,4,3,2,1])

# 使用value_counts()函数
# 计算df["R"]每个区间的值，赋值给R_value
#R_value = df["R"].value_counts()

# 计算各部分人数对应的比例，赋值给R_percent
#R_percent = R_value / 51394

# 使用print输出df["R"]
#print(df["R"])
# 输出R_value
#print(R_value)
# 输出R_percent
#print(R_percent)

# 2. 划分F
# 使用qcut()函数，将"order_count"的数据分箱
# 均分为5组，赋值给df["F"]
#df["F"] = pd.qcut(df["order_count"],q=5)

# 使用value_counts()函数
# 计算df["F"]每个区间的值，赋值给F_value
#F_value = df["F"].value_counts()

# 计算各部分人数对应的比例，赋值给F_percent
#F_percent = F_value / 51394

# 输出F_value
#print(F_value)

# 输出F_percent
#print(F_percent)

# 2. 划分F
# 使用qcut()函数，将"order_count"的数据分箱
# 均分为5组，区间标记命名为1-5，赋值给df["F"]
#df["F"] = pd.qcut(df["order_count"],q=5,labels=[1,2,3,4,5])

# 输出此时的df["F"]
#print(df["F"])

# 3. 划分M
# 使用qcut()函数，将"total_amount"的数据分箱
# 均分为5组，赋值给df["M"]
df["M"] = pd.qcut(df["total_amount"],q=5)

# 使用value_counts()函数
# 计算df["M"]每个区间的值，赋值给M_value
M_value = df["M"].value_counts()

# 计算各部分人数，对应的比例，赋值给M_percent
M_percent = M_value / 51394

# 输出M_value
print(M_value)

# 输出M_percent
print(M_percent)